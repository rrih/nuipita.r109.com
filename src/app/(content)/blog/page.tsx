import Link from "next/link";import {posts} from "@/content/blog/posts";import {ManualAd} from "@/components/ads/ManualAd";import {adsConfig} from "@/config/ads";export const metadata={title:"ぬい服・ぬいポーチのサイズ選び｜ぬいぴたノート"};export default function BlogIndex(){return <><h1>ぬいぴたノート</h1><p>ぬい服とポーチのサイズ選びを、採寸から考える読みものです。</p><div>{posts.map((post,index)=><div key={post.slug}>{index===4&&<ManualAd slot={adsConfig.slots.blogIndex}/>}<article className="card stitch"><span className="pill">{post.category}</span><h2><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p>{post.description}</p><p className="small">{post.dateModified}・読了目安 {post.readMinutes}分</p></article></div>)}</div></>}
