"use client";import Link from "next/link";import {useState} from "react";
export default function ProductLayout({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false);return <div className="shell"><header className="top"><button aria-label="メニュー" className="secondary" onClick={()=>setOpen(true)}>☰</button><span className="brand">ぬいぴた</span><span className="avatar" aria-label="ぬい未選択">●</span></header>{open&&<div className="drawer" onClick={()=>setOpen(false)}><aside onClick={e=>e.stopPropagation()} role="dialog" aria-label="メニュー"><button className="secondary" onClick={()=>setOpen(false)}>閉じる</button>{[["/install","インストール"],["/guide","使い方"],["/about","このサイトについて"],["/privacy","プライバシー"],["/terms","利用規約"],["/contact","お問い合わせ"]].map(([href,label])=><Link key={href} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</aside></div>}<main className="main">{children}</main><nav className="nav" aria-label="機能"><Link href="/nui" aria-current={false}>カルテ</Link><Link href="/">服</Link><Link href="/pouch">ポーチ</Link></nav></div>}
