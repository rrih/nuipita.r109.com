import {ImageResponse} from "next/og";import {getPost} from "@/content/blog/posts";export async function GET(_request:Request,{params}:{params:Promise<{slug:string}>}){const post=getPost((await params).slug);return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"center",padding:80,background:"#fff8f2",color:"#493c38",fontFamily:"sans-serif"}}><div style={{fontSize:34}}>ぬいぴたノート</div><div style={{fontSize:54,marginTop:30}}>{post?.title||"ぬいぴた"}</div><div style={{fontSize:28,marginTop:28}}>{post?.description||"ぬい服とポーチのサイズ選び"}</div></div>,{width:1200,height:630})}
