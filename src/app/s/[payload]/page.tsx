import Link from "next/link";import {decodeShare} from "@/lib/share";export const dynamic="force-dynamic";export async function generateMetadata(){return {robots:{index:false,follow:true}}}export default async function Share({params}:{params:Promise<{payload:string}>}){const {payload}=await params;const data=decodeShare(payload);return <div className="shell"><main className="main"><h1>{data?"共有された結果":"リンクを読み取れませんでした"}</h1>{data?<div className="card"><p>ぬいぴたの判定結果を確認できます。</p><Link href="/"><button>自分のぬいでも見る</button></Link></div>:<Link href="/"><button>ぬいぴたを使う</button></Link>}</main></div>}
