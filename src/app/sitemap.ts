import type {MetadataRoute} from "next";import {siteConfig} from "@/config/site";import {posts} from "@/content/blog/posts";export default function sitemap():MetadataRoute.Sitemap{return [...["/","/nui","/pouch","/guide","/install","/about","/privacy","/terms","/contact","/blog"].map(path=>({url:siteConfig.url+path,lastModified:new Date("2026-07-24")})),...posts.map(post=>({url:`${siteConfig.url}/blog/${post.slug}`,lastModified:new Date(post.dateModified)}))];}
