"use client";import {useState} from "react";import {garmentRegistry,type GarmentType} from "@/features/fit/garment-registry";export function GarmentChooser(){const [selected,setSelected]=useState<GarmentType>("pullover");return <fieldset className="card"><legend>服の形</legend><div className="actions">{(Object.keys(garmentRegistry) as GarmentType[]).map(type=><button type="button" className={selected===type?"":"secondary"} aria-pressed={selected===type} key={type} onClick={()=>setSelected(type)}>{garmentRegistry[type].label}</button>)}</div></fieldset>}
