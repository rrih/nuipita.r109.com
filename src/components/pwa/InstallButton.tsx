"use client";import {useEffect,useState} from "react";type PromptEvent=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:"accepted"|"dismissed"}>};export function InstallButton(){const [event,setEvent]=useState<PromptEvent|null>(null);useEffect(()=>{const handler=(value:Event)=>setEvent(value as PromptEvent);window.addEventListener("beforeinstallprompt",handler);return()=>window.removeEventListener("beforeinstallprompt",handler)},[]);async function install(){if(event){await event.prompt();setEvent(null)}else{setEvent(null)}}return <button onClick={install}>{event?"ホーム画面へ追加":"追加方法を見る"}</button>}
