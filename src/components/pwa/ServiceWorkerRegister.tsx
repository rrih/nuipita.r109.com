"use client";
import {useEffect,useState} from "react";
export function ServiceWorkerRegister(){const [updated,setUpdated]=useState(false);useEffect(()=>{if(!("serviceWorker" in navigator))return;let active=true;navigator.serviceWorker.register("/sw.js").then(registration=>{registration.addEventListener("updatefound",()=>{const worker=registration.installing;if(!worker)return;worker.addEventListener("statechange",()=>{if(active&&worker.state==="installed"&&navigator.serviceWorker.controller)setUpdated(true)})})}).catch(()=>undefined);return()=>{active=false}},[]);if(!updated)return null;return <div role="status" className="card" style={{position:"fixed",bottom:80,left:"50%",transform:"translateX(-50%)",zIndex:30,width:"min(92vw,400px)"}}><span>新しい表示があります。</span><div className="actions"><button onClick={()=>location.reload()}>再読み込み</button><button className="secondary" onClick={()=>setUpdated(false)}>あとで</button></div></div>}
