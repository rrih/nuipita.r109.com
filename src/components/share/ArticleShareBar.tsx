"use client";
export function ArticleShareBar({url,title}:{url:string;title:string}){const encodedUrl=encodeURIComponent(url);const encodedTitle=encodeURIComponent(title);return <div className="article-share" aria-label="記事を共有"><a className="share-x" href={`https://twitter.com/intent/tweet?url=${encodedUrl}&text=${encodedTitle}`} target="_blank" rel="noreferrer">Xで共有</a><a className="share-facebook" href={`https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`} target="_blank" rel="noreferrer">Facebook</a><a className="share-line" href={`https://line.me/R/msg/text/?${encodedTitle}%20${encodedUrl}`} target="_blank" rel="noreferrer">LINE</a></div>}
