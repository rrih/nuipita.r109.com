"use client";import {useState} from "react";import {shareCardSvg,svgToPng} from "@/lib/share-card";export function ShareActions({title,status,url}:{title:string;status:string;url:string}){const [busy,setBusy]=useState(false);async function shareImage(){setBusy(true);try{const blob=await svgToPng(shareCardSvg(title,status));const file=new File([blob],"nuipita-share.png",{type:"image/png"});if("share" in navigator&&"canShare" in navigator&&navigator.canShare({files:[file]})){await navigator.share({files:[file],url,text:"うちのぬいのサイズを見てみました。\n#ぬい活 #ぬいぴた"})}else{const objectUrl=URL.createObjectURL(blob);const link=document.createElement("a");link.href=objectUrl;link.download="nuipita-share.png";link.click();URL.revokeObjectURL(objectUrl)}}finally{setBusy(false)}}return <button className="secondary" disabled={busy} onClick={shareImage}>{busy?"画像を用意中":"画像を共有"}</button>}
