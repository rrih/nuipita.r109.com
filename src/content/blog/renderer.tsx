import type {BlogBlock} from "./types";export function BlogRenderer({blocks}:{blocks:BlogBlock[]}){return <>{blocks.map((block,index)=>{if(block.type==="heading")return block.level===3?<h3 key={index}>{block.text}</h3>:<h2 key={index}>{block.text}</h2>;if(block.type==="list")return <ul key={index}>{block.items?.map(item=><li key={item}>{item}</li>)}</ul>;if(block.type==="steps")return <ol key={index}>{block.items?.map(item=><li key={item}>{item}</li>)}</ol>;if(block.type==="callout")return <aside className="card stitch" key={index}>{block.text}</aside>;return <p key={index}>{block.text}</p>})}</>}
