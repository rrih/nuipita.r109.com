export type PouchStatus="noFit"|"snug"|"stable"|"moves";
export function judgePouch(items:{height:number;width:number;depth:number}[],inner:{height:number;width:number;depth:number},orientation:"upright"|"sideways"|"auto"){const evalOne=(o:"upright"|"sideways")=>{const widths=items.reduce((s,i)=>s+(o==="upright"?i.width:i.height),0)+Math.max(0,items.length-1)*.5;const height=Math.max(...items.map(i=>o==="upright"?i.height:i.width));const depth=Math.max(...items.map(i=>i.depth));const margins=[(inner.width-widths)/inner.width,(inner.height-height)/inner.height,(inner.depth-depth)/inner.depth];const min=Math.min(...margins);const status=widths>inner.width||height>inner.height||depth>inner.depth?"noFit":min<.03?"snug":margins.some(m=>m>.18)?"moves":"stable";return {orientation:o,width:widths,height,depth,margins,status} as const};const candidates=orientation==="auto"?[evalOne("upright"),evalOne("sideways")]:[evalOne(orientation)];return candidates.find(x=>x.status!=="noFit")??candidates[0];}
