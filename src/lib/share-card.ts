function escapeXml(value:string){return value.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&apos;")}
export function shareCardSvg(title:string,status:string,width=1080,height=1350){return `<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}"><rect width="100%" height="100%" rx="56" fill="#fff8f2"/><circle cx="${width-130}" cy="150" r="90" fill="#ffd7ce"/><text x="80" y="150" fill="#493c38" font-family="sans-serif" font-size="42" font-weight="700">ぬいぴた</text><text x="80" y="540" fill="#493c38" font-family="sans-serif" font-size="64" font-weight="700">${escapeXml(title)}</text><text x="80" y="660" fill="#f36f62" font-family="sans-serif" font-size="76" font-weight="700">${escapeXml(status)}</text><text x="80" y="1250" fill="#7f706a" font-family="sans-serif" font-size="28">寸法から見た目安</text></svg>`}
export async function svgToPng(svg:string,width=1080,height=1350){const canvas=document.createElement("canvas");canvas.width=width;canvas.height=height;const context=canvas.getContext("2d");if(!context)throw new Error("canvas");const image=new Image();const source=URL.createObjectURL(new Blob([svg],{type:"image/svg+xml"}));try{await new Promise<void>((resolve,reject)=>{image.onload=()=>resolve();image.onerror=()=>reject(new Error("image"));image.src=source});context.drawImage(image,0,0,width,height);return await new Promise<Blob>((resolve,reject)=>canvas.toBlob(blob=>blob?resolve(blob):reject(new Error("blob")),"image/png"))}finally{URL.revokeObjectURL(source);image.src="";canvas.width=0;canvas.height=0}}
