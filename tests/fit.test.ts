import {describe,expect,it} from "vitest";import {classify,fitRatio,judgeFit} from "@/features/fit/engine";
describe("fit engine",()=>{it("converts flat circumference only",()=>{expect(fitRatio(5,10,"flat",1)).toBe(1);expect(fitRatio(5,10,"circumference",1)).toBe(.5)});it("keeps all circumference boundaries",()=>{expect(classify(.939,"head")).toBe("blocked");expect(classify(.94,"head")).toBe("tight");expect(classify(1.02,"head")).toBe("fit");expect(classify(1.151,"head")).toBe("roomy")});it("prioritizes blocked and rounds score",()=>{const r=judgeFit({opening:8,chest:10,plushHead:10,plushBody:10,stretch:"none",softness:"firm"});expect(r.overall).toBe("blocked");expect(r.score%5).toBe(0)})});
