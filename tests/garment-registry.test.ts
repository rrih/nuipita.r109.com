import {describe,expect,it} from "vitest";import {garmentRegistry} from "@/features/fit/garment-registry";describe("garment registry",()=>{it("defines all supported garment shapes",()=>{expect(Object.keys(garmentRegistry)).toEqual(["pullover","frontOpen","bottoms","onePiece","headwear"])});it("keeps critical fields inside visible fields",()=>{for(const garment of Object.values(garmentRegistry))for(const field of garment.critical)expect(garment.fields).toContain(field)})});
