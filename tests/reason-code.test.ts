import {describe,expect,it} from "vitest";import {judgeFit} from "@/features/fit/engine";describe("fit reason codes",()=>{it("returns machine-readable reasons",()=>{const result=judgeFit({opening:8,plushHead:10,stretch:"none",softness:"firm"});expect(result.parts[0]?.reasonCode).toBe("HEAD_OPENING_TOO_SMALL")});it("does not return UI sentences",()=>{const result=judgeFit({length:7,plushLength:10,stretch:"none",softness:"firm"});expect(result.parts[0]?.reasonCode).toBe("LENGTH_SHORT")})});
