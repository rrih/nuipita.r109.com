import {describe,expect,it} from "vitest";import {STORE_LIMITS,validateBackup} from "@/lib/storage";describe("storage contract",()=>{it("keeps required store limits",()=>{expect(STORE_LIMITS).toMatchObject({profiles:30,garmentDrafts:10,pouchDrafts:10,results:20})});it("rejects malformed backups",()=>{expect(validateBackup({version:1,profiles:[{name:"x"}]})).toBe(false);expect(validateBackup({version:2,profiles:[]})).toBe(false)})});
